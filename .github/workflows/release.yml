name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version in X.Y.Z format"
        required: true
        type: string
      dry_run:
        description: "Validate the full release flow without committing, tagging, pushing, or publishing"
        required: true
        default: true
        type: boolean

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on:
      - self-hosted
      - Windows
      - X64
      - amps-release
    env:
      RELEASE_VERSION: ${{ github.event.inputs.version }}
      RELEASE_TAG: v${{ github.event.inputs.version }}
      RELEASE_DRY_RUN: ${{ github.event.inputs.dry_run }}
    steps:
      - name: Checkout
        shell: bash
        run: |
          set -euo pipefail

          git init .
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          git fetch origin "+refs/heads/main:refs/remotes/origin/main" --tags --prune
          git checkout -B main refs/remotes/origin/main

      - name: Configure authenticated git remote
        shell: bash
        env:
          RELEASE_PUSH_TOKEN: ${{ secrets['RELEASE_PUSH_TOKEN'] }}
          GITHUB_PUSH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          PUSH_TOKEN="${RELEASE_PUSH_TOKEN:-}"
          if [[ "${RELEASE_DRY_RUN}" != "true" && -z "${PUSH_TOKEN}" ]]; then
            echo "RELEASE_PUSH_TOKEN is required for non-dry-run workflow releases."
            exit 1
          fi

          if [[ -z "${PUSH_TOKEN}" ]]; then
            PUSH_TOKEN="${GITHUB_PUSH_TOKEN}"
            echo "Using GITHUB_TOKEN for dry-run git operations."
          else
            echo "Using RELEASE_PUSH_TOKEN for git fetch/push operations."
          fi
 
          git remote set-url origin "https://x-access-token:${PUSH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          echo "GH_TOKEN=${PUSH_TOKEN}" >> "$GITHUB_ENV"
 
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Validate release input and repository state
        shell: bash
        run: |
          set -euo pipefail
 
          if [[ ! "${RELEASE_VERSION}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Version must match X.Y.Z, got '${RELEASE_VERSION}'."
            exit 1
          fi

          git fetch origin main --tags --prune

          if [[ "$(git rev-parse HEAD)" != "$(git rev-parse origin/main)" ]]; then
            echo "Run this workflow from the latest main commit."
            exit 1
          fi

          if git rev-parse -q --verify "refs/tags/${RELEASE_TAG}" >/dev/null; then
            echo "Tag ${RELEASE_TAG} already exists."
            exit 1
          fi

      - name: Validate strict release runner
        shell: bash
        run: |
          set -euo pipefail

          if [[ ! -d ../amps-c++-client-5.3.5.1-Windows ]]; then
            echo "Strict release runner is missing ../amps-c++-client-5.3.5.1-Windows."
            exit 1
          fi

          for tool in git go make gh powershell; do
            if ! command -v "${tool}" >/dev/null 2>&1; then
              echo "Required tool '${tool}' is not available on the release runner."
              exit 1
            fi
          done

      - name: Run scripted release
        shell: pwsh
        env:
          GH_TOKEN: ${{ env.GH_TOKEN }}
        run: |
          $arguments = @('-Version', $env:RELEASE_VERSION, '-Yes')
          if ($env:RELEASE_DRY_RUN -eq 'true') {
            $arguments += '-DryRun'
          }
          & .\release.local.ps1 @arguments

      - name: Vulnerability scan (advisory)
        continue-on-error: true
        run: make vuln-scan

      - name: Release summary
        shell: bash
        run: |
          {
            if [[ "${RELEASE_DRY_RUN}" == "true" ]]; then
              echo "## Dry run succeeded"
              echo
              echo "No commit, tag, push, or GitHub Release was created."
            else
              echo "## Release created"
            fi
            echo
            echo "- Version: ${RELEASE_VERSION}"
            echo "- Tag: ${RELEASE_TAG}"
            echo "- Dry run: ${RELEASE_DRY_RUN}"
            echo "- Path: release.local.ps1"
            echo "- Runner: self-hosted Windows/X64 amps-release"
            echo "- Parity check: required"
            if [[ "${RELEASE_DRY_RUN}" != "true" ]]; then
              echo "- Release: https://github.com/${GITHUB_REPOSITORY}/releases/tag/${RELEASE_TAG}"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
